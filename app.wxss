/* 一些测试样式 */

.box {
  padding: 20rpx 40rpx;
  /* margin: 20rpx; */
  /* padding: 40rpx 20rpx; */
  border-radius: 20rpx;
  background: #fff;
}
.main-title {
  padding: 50rpx 20rpx 20rpx 40rpx;
  font-size: 22px;
  /* font-weight: bold; */
  color: #333;
}
.flex-column {
  display: flex;
  flex-direction: column;
}
.flex-row {
  display: flex;
  align-items: center;
}

.font16 {
  font-size: 16px;
  color: #666;
}
.font18 {
  font-size: 18px;
  color: #666;
}

page,
.main-container {
  width: 750rpx;
  min-height: 100%;
  overflow-x: hidden;
  /* background:#f4f4f4; */
}

/* ios 下底部 margin-bottom 失效 */
/* Page {
  padding-bottom: 160rpx;
} */

/*右下角 添加项目 悬浮按钮*/
.add-btn {
  position: fixed;
  right: 60rpx;
  bottom: 60rpx;
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(9, 187, 7, 0.2);
}

.add-btn image {
  width: 60rpx;
  height: 60rpx;
}

/*页面下部悬浮按钮*/
.createM {
    position: fixed;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 140rpx;
    font-weight: bold;
    background: #fff;
    border-top: 2rpx solid #eee;
    z-index: 2;
}

.createM button {
    position: absolute;
    bottom: 24rpx;
    margin-left: 5%;
    width: 90%;
}

/* 颜色 */
.c-doing {
  background: #7fabda;
}
.c-coming {
  /* background: #EB7159; */
  background: #efce4a;
}
.c-completed {
  background: #d75a4a;
}
